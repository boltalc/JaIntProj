## Basic Calculator

- I created a class with methods for each arithmetic operations
- The calculator accepts user input
- The user decides the arithmetic operation they want to perform first, then the digits to calculated are entered next
- I colour coded the instructions to make use easier